#include <iostream>
#include <vector>
#include <random>
#include <algorithm>
using namespace std;
int minigame() {
    
}
void checkchips(head) {
    current = head;
    while current != tail {
        if current.value == 0:
            minigame();
        current = current.next
    }
}
