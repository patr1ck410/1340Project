#include <iostream>
#include <string>
using namespace std;

void checkwin(player * button) { //check which type of poker hand player have
	player * current = button;
	do {
		if (current ->ingame){
		`		


		current = curent -> next;
	}while(current!= button);
}
